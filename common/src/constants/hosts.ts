/**
 * Prod origin of the obitobuff.com web app (marketing site + browser sign-in).
 * The CLI and Obitobuff Desktop both send users here for the device-code login
 * flow; sharing the literal keeps their prod defaults from drifting.
 */
export const OBITOBUFF_WEB_URL_PROD = 'https://obitobuff.com'
